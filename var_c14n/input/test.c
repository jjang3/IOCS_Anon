#include <stdio.h>
#include <stdlib.h>

int main() {
    // // Declare an integer
    // int a = 5;

    // // Declare a character array of 100 bytes
    // char b[100];

    // // Initialize the array with 'A'
    // for(int i = 0; i < 5; i++) {
    //     b[i] = 'A';
    // }

    // printf("%c",b[11]);
    // // Declare a float
    float c = 20.0;

    // int d = a + 8;

    c = 17.0;

    float d = c +2.0;

    printf("%f", c);

    // // Print the addresses of the variables
    // printf("Address of int a: %p\n", (void*)&a);
    // printf("Address of char array b: %p\n", (void*)b);
    // printf("Address of float c: %p\n", (void*)&c);

    // // Calculate and print the distance between variables in memory
    // printf("\nDistance between a and b: %ld bytes\n", (long)abs((char*)&a - (char*)b));
    // printf("Distance between b and c: %ld bytes\n", (long)abs((char*)&c - (char*)b + sizeof(b)));

    return 0;
}
